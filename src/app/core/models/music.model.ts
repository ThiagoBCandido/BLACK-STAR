export interface Track {
    id: number;
    title: string;
    artist: string;
    album: string;
    cover: string;
    duration: string;
}

export interface Playlist {
    title: string;
    description: string;
    image: string;
}